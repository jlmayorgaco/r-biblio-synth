% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/public_api_docs.R
\name{run_pipeline}
\alias{run_pipeline}
\alias{run_m0}
\alias{run_m1}
\alias{run_m2}
\alias{run_m3}
\alias{run_bslr}
\alias{bslr_protocol_template}
\alias{validate_bslr_protocol}
\alias{bslr_protocol_to_search_metadata}
\alias{bslr_assemble_paper}
\alias{validate_pipeline_result}
\title{RBiblioSynth pipeline runners}
\usage{
run_pipeline(sources, modules = c("m0", "m1", "m2", "m3"), config = biblio_config(), prisma_spec = NULL, export = TRUE)
run_m0(sources, config = biblio_config(), prisma_spec = NULL, screening_ledger = NULL, search_metadata = NULL, enrich = FALSE, export = TRUE)
run_m1(input, config = biblio_config(), export = TRUE)
run_m2(input, config = biblio_config(), export = TRUE)
run_m3(input, config = biblio_config(), export = TRUE)
run_bslr(sources, protocol, screening_ledger = NULL, prisma_spec = "auto", modules = c("m1", "m2", "m3"), config = biblio_config(), export = TRUE)
bslr_protocol_template(title = "B-SLR Study")
validate_bslr_protocol(protocol)
bslr_protocol_to_search_metadata(protocol)
bslr_assemble_paper(bslr_result, config = biblio_config(), render = TRUE)
validate_pipeline_result(x)
}
\arguments{
\item{sources}{Named list of bibliographic source specifications.}

\item{modules}{Character vector selecting which public modules to run.}

\item{config}{Configuration list created with [biblio_config()].}

\item{prisma_spec}{Optional PRISMA specification for `run_m0()`.}

\item{screening_ledger}{Optional screening decisions ledger used by `run_m0()`
and `run_bslr()`.}

\item{search_metadata}{Optional human-defined methodological metadata passed to
`run_m0()`.}

\item{enrich}{Logical or character vector enabling M0 enrichment providers.}

\item{export}{Logical; when `TRUE`, write artifacts to disk.}

\item{input}{Module input object. For `run_m1()` and `run_m3()`, this is a
bibliographic data frame. For `run_m2()`, this is an annual production
table.}

\item{protocol}{A B-SLR protocol list or a JSON/YAML file path.}

\item{bslr_result}{Result object returned by [run_bslr()].}

\item{render}{Logical; when `TRUE`, attempt to render the paper bundle with
Quarto if the executable is available.}

\item{title}{Title used in a generated B-SLR protocol template.}

\item{x}{Object to validate.}
}
\value{
`run_pipeline()` returns a `biblio_pipeline_result`. Each `run_m*()`
  function returns a `biblio_module_result`. `run_bslr()` returns a
  `biblio_module_result` with nested `M0`-`M3` outputs plus methodological
  checkpoints, bibliometric mapping, and SLR/theorising scaffolds.
  `bslr_assemble_paper()` returns a paper-bundle descriptor containing Quarto
  and LaTeX sources plus any rendered outputs.
}
\description{
Execute the hardened M0-M3 core either module by module or through the
official end-to-end pipeline entrypoint. For methodology-heavy reviews, use
the dedicated B-SLR workflow helpers, including journal-style paper
assembly.
}
